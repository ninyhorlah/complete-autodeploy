version: 2.1

jobs:
  build-frontend:
    docker:
      - image: cimg/node:14.4.0
    steps:
      - checkout
      - run:
          name: build frontend
          command: |
            cd newsily
            npm install
            npm build

workflows:
  complete-autodeploy:
    jobs:
      - build-frontend
